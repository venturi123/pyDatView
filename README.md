# pyDatView

## QuickStart
```bash
sudo apt search wxgtk  # first search the package like `python3-wxgtk4.0`
sudo apt install python3-wxgtk4.0  # install the package like `python3-wxgtk4.0` for Ubuntu 22.04

git clone https://github.com/venturi123/pyDatView.git
pipx install --system-site-packages .
```
